# Download files

Add these files here for the thank-you pages to work:

- **Should-you-go-public-guide.pdf** — Guide PDF (download from `/guide/thank-you`)
- **Founders-reset-audio.mp3** — Audio file (download from `/leap/thank-you`)

Files in `public/downloads/` are served at `/downloads/` in the browser.
