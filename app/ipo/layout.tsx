import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "IPO Advisory | MandyC.",
}

export default function IPOLayout({ children }: { children: React.ReactNode }) {
  return children
}
